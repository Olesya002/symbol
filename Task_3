%1
syms x;
F = diff(atan(x)/2-x/(2*(1+x^2)^2));
disp(simplify(F));

%2
syms x;
F = diff(3*x^4-14*x^3+12*x^2+24*x+6);
disp(simplify(F));

%3
syms x;
F = diff((sin(3*x)-cos(3*x))^2);
disp(simplify(F));
